spring:
  datasource:
    url: jdbc:oracle:thin:@localhost:1521/XEPDB1
    username: system
    password: oracle
    driver-class-name: oracle.jdbc.OracleDriver
  
    baseline-version: 0

  rabbitmq:
    host: localhost
    port: 5672
    username: user
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

  cache:
    type: simple



